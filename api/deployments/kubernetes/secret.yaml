apiVersion: v1
kind: Secret
metadata:
  name: lexure-mvp-secrets
  namespace: lexure-mvp
  labels:
    app: payment-watchdog-mvp
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # echo -n "your_actual_password" | base64
  database_password: cGFzc3dvcmQ=  # "password" in base64
  stripe_secret_key: ""  # Add your Stripe secret key in base64
  stripe_webhook_secret: ""  # Add your Stripe webhook secret in base64
  email_password: ""  # Add your email password in base64
