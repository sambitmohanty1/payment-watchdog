apiVersion: v1
kind: Secret
metadata:
  name: payment-watchdog-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:${DB_PASSWORD}@postgres:5432/payment_watchdog?sslmode=disable"
  REDIS_URL: "redis://redis:6379"
  REDIS_PASSWORD: "${REDIS_PASSWORD}"
  APP_ENV: "production"
  # Add other API/Web secrets needed here
