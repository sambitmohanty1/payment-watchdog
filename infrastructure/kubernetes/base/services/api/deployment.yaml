apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-watchdog-api
  labels:
    app.kubernetes.io/name: payment-watchdog-api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: payment-watchdog
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-watchdog-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-watchdog-api
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: payment-watchdog
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8085"
    spec:
      serviceAccountName: payment-watchdog-api
      containers:
      - name: app
        image: ghcr.io/sambitmohanty1/payment-watchdog/api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8085
          name: http
        envFrom:
        - configMapRef:
            name: payment-watchdog-config
        - secretRef:
            name: payment-watchdog-secrets
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 8085
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
          readOnly: true
      volumes:
      - name: config-volume
        configMap:
          name: payment-watchdog-config
          optional: false
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                  - payment-watchdog-api
              topologyKey: "kubernetes.io/hostname"
