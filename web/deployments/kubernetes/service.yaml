apiVersion: v1
kind: Service
metadata:
  name: payment-watchdog-web
  namespace: lexure
  labels:
    app: payment-watchdog
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: payment-watchdog
    component: web
